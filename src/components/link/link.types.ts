// Link.types.ts
import { AnchorHTMLAttributes } from 'react';

export interface LinkProps extends AnchorHTMLAttributes<HTMLAnchorElement> {
  // Extend the native anchor element props and add any custom props for your component
}