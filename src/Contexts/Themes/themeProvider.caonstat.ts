import {
  background,
  color,
  spacing,
  typography,
  strokes,
  shadow,
  shadowDeep,
  neumorphismShadow,
  breakpoint,
  pageMargin,
} from "@shared/styles";
import { Theme } from "./themeProvider.types";

export const defaultTheme: Theme = {
  background,
  color,
  spacing,
  typography,
  strokes,
  shadow,
  shadowDeep,
  neumorphismShadow,
  breakpoint,
  pageMargin,
};
